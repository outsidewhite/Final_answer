# 課題1 Webスクレイピング

検索条件は「名古屋駅（名駅）」「居酒屋」です。

```powershell
python -m pip install -r requirements.txt
python 1-1.py
python 1-2.py
```

`1-2.py` は同じディレクトリの `chromedriver.exe` を使用します。ChromeDriverは同梱したバージョンに対応するGoogle Chromeで実行してください。
